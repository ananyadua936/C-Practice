#include <bits/stdc++.h>
using namespace std;
int main()
{
    vector<int> v = {1, 3, 5, 7, 9};
    stack<int> s;
    for (auto i : v)
        s.push(i);
    v.clear();
    while (!s.empty()) {
        v.push_back(s.top());
        s.pop();
    }
    for (auto i : v)
        cout << i << " ";
    return 0;
}
